package id.lua.absensikerja.model.enums;

/** Jenis upah pada Openshift. */
public enum JenisUpahOpenshift {
    /** Upah mengikuti pengaturan upah lembur karyawan. */
    SESUAI_LEMBUR,
    /** Upah nominal (per jam) ditentukan eksplisit. */
    NOMINAL
}
